# NSL_Simulation
### Installation (Ubuntu/Linux)

Before installing python packages, build-essential and PostgreSQL development libraries are required:
```bash
sudo apt-get update
sudo apt-get install -y build-essential python3-dev libpq-dev
pip3 install -r requirements.txt
